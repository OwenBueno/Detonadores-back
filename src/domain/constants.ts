export const TICK_INTERVAL_MS = 50;
export const TICKS_PER_SECOND = 1000 / TICK_INTERVAL_MS;

/** Wall-clock grace period before a disconnected player is forfeited (US-025). */
export const RECONNECT_GRACE_MS = 25_000;
