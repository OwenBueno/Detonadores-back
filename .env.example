# HTTP server port (default 3001)
PORT=3001

# Postgres connection string (optional until persistence is wired)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Redis connection string (optional until live state adapter is used)
# REDIS_URL=redis://localhost:6379
